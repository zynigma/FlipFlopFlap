using UnityEngine;
using System.Collections;

public class Tryout : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log ("This does work, can't wait to get it ALL working right!");
	}
	
	// Update is called once per frames
	void Update () {
	
	}
}
